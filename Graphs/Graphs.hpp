#include <bits/stdc++.h>
using namespace std;

struct Graph
{
    // Adjacency matrix in order to represent the graph
    // @Pair.first = node id
    // @Pair.second = weight of the edge
    vector< vector< pair<int,int > > Graph;
};